# Dataset Files Directory

This directory is for local dataset files (for development/testing purposes only).

**Note:** Large dataset files are NOT tracked in git. All production datasets are hosted on SharePoint and referenced via URLs in `server.js`.

## Local Development
If you need to test file downloads locally, place your test files here. They will be ignored by git.
